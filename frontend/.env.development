# Opção 1: Usando Proxy do Vite (Padrão)
# Requer que o backend esteja rodando em localhost:8010 ou que o proxy em vite.config.ts esteja apontando para o servidor correto
VITE_API_URL=/api/v1

# Opção 2: Conexão Direta Local (Sem Proxy)
# VITE_API_URL=http://localhost:8010/api/v1

# Opção 3: Conexão Direta com VPS (Produção)
# Use esta opção se quiser testar o frontend localmente conectando ao banco de produção
# VITE_API_URL=http://imperiodasrifas.app.br:8010/api/v1